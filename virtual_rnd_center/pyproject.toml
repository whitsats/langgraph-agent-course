[project]
name = "virtual_rnd_center"
version = "0.1.0"
description = "virtual_rnd_center using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.14.4"
]

[project.scripts]
kickoff = "virtual_rnd_center.main:kickoff"
run_crew = "virtual_rnd_center.main:kickoff"
plot = "virtual_rnd_center.main:plot"
run_with_trigger = "virtual_rnd_center.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "flow"
